#include <stdio.h>

int main()  
{
   int rows,i,j;
   printf("Enter number of rows=");
   scanf("%d",&rows);
   for(i=1;i<=rows;i++)   
   {
       for(j=0;j<2*i;j++)
       printf("%d",j%2);
       printf("\n");
   }
   return 0;
}
